pipeline {
  environment {
    registry = "jmaclean/docker_hub_account/poc-trigger-by-tag"
    registryCredential = 'futurice-jmaclean-docker'
    dockerImage = ''
  }
  agent any
  stages {
    stage ('Process all environment variables') {
      steps {
        sh 'echo "Process all environment variables \
        and ectra line"'
      }
    }
    stage ('Close-out') {
      steps {
        sh 'echo HERE'
      }
    }
  }
}
