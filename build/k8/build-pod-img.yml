kind: Pod
metadata:
  name: img
  annotations:
    container.apparmor.security.beta.kubernetes.io/img: unconfined
    container.seccomp.security.alpha.kubernetes.io/img: unconfined
spec:
  containers:
  - name: img
    workingDir: /home/jenkins
    image: r.j3ss.co/img:0.5.6
    imagePullPolicy: Never
    # securityContext:
    #     rawProc: true
    #     privileged: true
    command:
    - cat
    tty: true
