@Library("cicd@develop") _cicd

// Global settings. Can be overwritten locally if needed.
def cicd = initCicd()

println cicd
println "My build: " + cicd.build.number

pipeline {
    agent { label 'master' }
    stages {

        stage('Build1') {
            steps {
               sh 'echo "hello world1" '
            }  
        }
    }
}
// def yamlData = yamlParser()