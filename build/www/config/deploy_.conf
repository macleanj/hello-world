# Configuration automatically mapped to deployment environment sandbox (used for local developement)
# $DEPLOY_TAG is unset on the local developement enviroment. $DEPLOY_TAG will be set automatically to the git "branch-tag" on CICD pipeline execution.
ENVIRONMENT=Futurice
NAMESPACE=wltp-sandbox